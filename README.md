# Simple graphql/mongo/mongoose app

## Bootstrap

Instal dependencies

`cd backend && npm install`
`cd frontend-v && npm install`

### backend

Set mongo url in `backend/.env`

Run `npm run dev`

### Frontend

Run `npm run dev`
